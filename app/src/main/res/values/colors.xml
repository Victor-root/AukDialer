<?xml version="1.0" encoding="utf-8"?>
<resources>
    <color name="black">#FF000000</color>
    <color name="white">#FFFFFFFF</color>
    <color name="auk_window_background">#FFF8F9FF</color>
    <!-- The splash icon is a flat white glyph, so this needs contrast against it rather
         than matching the window background. Kept neutral (not tied to a hue) so it never
         clashes with whichever launcher colour is active, and the same in light and dark
         (see values-night/themes.xml) since the glyph itself doesn't change. -->
    <color name="auk_splash_background">#FF111418</color>
</resources>
